# Birth Binding Prototypes

Experimental Compact package that applies all Midnight VC holder-binding
profiles to the same birth-credential claim set.

This package is intentionally prototype-oriented. It does not replace the
production `credentials-birth` or `credentials-birth-secret` packages.
