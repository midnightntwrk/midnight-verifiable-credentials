pragma language_version >= 0.20;

import CompactStandardLibrary;

include "../../../../core/primitives/credentials/src/credentials";
include "./mixed-claims-credential/claims";
include "./mixed-claims-credential/model";

import VC<MixedClaimsCredentialClaims, ExplicitHolderBinding, NoStatusBinding>;
import VP<MixedClaimsDisclosures, ExplicitHolderBinding>;
import CredentialPresentationRelations<
  MixedClaimsCredentialClaims,
  MixedClaimsDisclosures,
  ExplicitHolderBinding,
  NoStatusBinding
> prefix MixedClaimsPresentation_;

export type MixedClaimsCredential = Credential;
export type MixedClaimsPresentation = Presentation;
include "./mixed-claims-credential/helpers";
