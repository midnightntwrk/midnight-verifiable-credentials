pragma language_version >= 0.20;

import CompactStandardLibrary;

include "../../../../core/primitives/credentials/src/credentials";
include "./hello-family-credential/claims";
include "./hello-family-credential/model";

import VC<HelloFamilyClaims, OffchainMidnightHolderBinding, NoStatusBinding>;
import VP<HelloFamilyDisclosures, OffchainMidnightHolderBinding>;
import CredentialPresentationRelations<
  HelloFamilyClaims,
  HelloFamilyDisclosures,
  OffchainMidnightHolderBinding,
  NoStatusBinding
> prefix HelloFamilyOffchainPresentation_;

export type HelloFamilyOffchainCredential = Credential;
export type HelloFamilyOffchainPresentation = Presentation;
// NOTE: helpers come last because they reference the exported aliases above.
include "./hello-family-credential/offchain-helpers";
