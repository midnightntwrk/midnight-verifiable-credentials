# 2026-05-20 Backlog Collapse Archive

These files preserve the pre-collapse backlog and audit text from `origin/develop`
before the active VC queue was reduced to `docs/plans/repository-audit-backlog.md`
plus narrow maturity and university index files.

Use this archive for historical PR-number traces, VC-MAT item history, and old
university develop-port notes. Use the current repository audit backlog for new
work planning.

One internal link in `vc-maturity-backlog.pre-collapse.md` was redirected from
`repository-audit-backlog.md` to `repository-audit-backlog.pre-collapse.md` so
the archived snapshot remains navigable and `pnpm run docs:links` stays green.
