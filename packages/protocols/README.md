# Protocols

This top-level area is reserved for wire-level or interoperability protocol
bindings.

Target contents:
- OIDC
- DIDComm
- custom API bindings

Protocols define binding and transport-facing shapes. They are not the canonical
VC core and should not own business-flow orchestration.
