pragma language_version >= 0.20;

import CompactStandardLibrary;

include "../../../../../packages/core/primitives/credentials/src/credentials";
include "./dummy-claims-credential/claims";
include "./dummy-claims-credential/model";

import VC<DummyClaims, NoClaimCommitments, ExplicitHolderBinding, NoStatusBinding>;
import VP<DummyClaimsDisclosures, ExplicitHolderBinding>;
import CredentialPresentationRelations<
  DummyClaims,
  NoClaimCommitments,
  DummyClaimsDisclosures,
  ExplicitHolderBinding,
  NoStatusBinding
> prefix DummyClaimsPresentation_;

export type DummyClaimsCredential = Credential;
export type DummyClaimsPresentation = Presentation;
include "./dummy-claims-credential/helpers";
