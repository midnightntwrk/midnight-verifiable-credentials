pragma language_version >= 0.20;

import CompactStandardLibrary;

include "../../../../../packages/core/primitives/credentials/src/credentials";
include "./hello-family-credential/claims";
include "./hello-family-credential/model";

import VC<HelloFamilyClaims, NoClaimCommitments, ExplicitHolderBinding, NoStatusBinding>;
import VP<HelloFamilyDisclosures, ExplicitHolderBinding>;
import CredentialPresentationRelations<
  HelloFamilyClaims,
  NoClaimCommitments,
  HelloFamilyDisclosures,
  ExplicitHolderBinding,
  NoStatusBinding
> prefix HelloFamilyPresentation_;

export type HelloFamilyCredential = Credential;
export type HelloFamilyPresentation = Presentation;
// NOTE: helpers come last because they reference the exported aliases above.
include "./hello-family-credential/helpers";
