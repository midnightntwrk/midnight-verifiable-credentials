pragma language_version >= 0.20;

import CompactStandardLibrary;

// Standalone family root: own the canonical shared core exactly once, then add
// the dependency-free family composition surface.
include "../../../../../packages/core/compact/src/credentials/composable";
include "./hello-family-credential/composable";
