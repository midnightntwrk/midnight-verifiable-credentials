pragma language_version >= 0.20;

import CompactStandardLibrary;

include "credentials";

export pure circuit validateReleaseCandidateSchema(
  schema: SchemaRef
): Uint<16> {
  assertValidSchemaRef(schema);
  return schema.majorVersion;
}
