{
  "name": "credential-compact-consumer",
  "private": true,
  "type": "module",
  "dependencies": {
    "@midnight-ntwrk/credential-compact": "file:./vendor/package.tgz"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "test:node": "node --input-type=module -e \"import('@midnight-ntwrk/credential-compact').then(({ pureCircuits }) => { if (typeof pureCircuits !== 'object') process.exit(1); })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
