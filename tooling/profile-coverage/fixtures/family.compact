pragma language_version >= 0.19 && <= 0.19;

// Tooling-only family root for deterministic profile-composition coverage.
export pure circuit profileCoverageFixtureClaim(value: Field): Field {
  return value;
}
