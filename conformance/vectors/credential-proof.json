{
  "formatVersion": 1,
  "category": "credential-proof",
  "positive": [
    { "id": "verify-canonical-credential-body-root", "operation": "verify-precomputed-root" }
  ],
  "negative": [
    { "id": "reject-substituted-credential-body-root", "operation": "verify-precomputed-root", "mutation": "body-root", "errorIncludes": "Credential body root does not match credential" }
  ]
}
